# S6 round 4 — correlated reference laws and real-q control of dependence

## Before working

This is the NEXT assignment after the already-dispatched round-three task.
Start it only after that task has finished. Do not interrupt or duplicate a
running attempt. Read [TARGET.md](TARGET.md), [KNOWN_RESULTS.md](KNOWN_RESULTS.md),
[PITFALLS.md](PITFALLS.md), and [CONTRACT.md](CONTRACT.md); all are mandatory.
Use [READING_MAP.md](READING_MAP.md) to select optional proofs and exact fixtures.

The task below continues your lane; it does not ask you to repeat the last
assignment. First compare the most recent result in THIS conversation with
the supplied reviewed baseline. In `DELTA_FROM_LAST_ROUND.md`, give its exact
claim, which parts are merely author claims versus independently reviewed,
the obstruction it resolved, and one genuinely unpaid next estimate. If no
new local result is available, use the unpaid gates below. If the main gate
below was just solved, move to the next uncovered growing-family/parameter
case or the quantified missing-error step; do not re-prove it for credit.
Preserve the main target. A new auxiliary candidate must be stated and tested
explicitly, not presented as known. Never treat an unavailable result from a
different task as a premise. Record the snapshot/commit you actually read.


The reviewed second round rigorously disposed of unrestricted Berezin/Jensen and
showed why an optimized product tangent misses order-n spatial dependence.
Now seek a method that RETAINS this dependence and controls its curvature.
Do not spend this round re-disproving Berezin positivity, reproducing the
q=2 certificate, or rediscovering the product-tangent gap.

For 0<K_n(a)<I, let L_n=K_n(I-K_n)^-1 and
p_a(S)=det(I-K_n) det((L_n)_S). For real q near 1,
Z_n(q,a)=sum_S p_a(S)^q and H_n(a)=-partial_q log Z_n(1,a).
At finite n these are exact. Integer replicas, an uncontrolled saddle point,
or a spectral free-fermion entropy formula cannot replace the real-q measured
configuration problem. No uniform limit/derivative interchange is supplied.

Scout at most three mechanisms, then deepen at most two:
1. Replace the product reference by a correlated spatial reference law w_(a,R)
   built from consecutive block probabilities or genuine finite-memory
   conditionals of the true process. Derive the exact positive-measure
   variational/relative-entropy remainder and ALL derivatives of the moving
   reference. Try to bound its curvature, or a signed finite Jensen defect,
   as the block/memory radius R grows. A standard identity alone is baseline.
2. A convergent connected-cluster/cumulant or resolvent expansion for the
   principal-minor pressure at real q near 1 which keeps the nonproduct terms.
   Specify the expansion parameter, summability criterion, domain, and error
   under the needed q and a derivatives. In the sine process, decay of pair
   correlations does not itself prove convergence of a full entropy expansion.
3. A positive integral/interpolation/variational representation from classical
   statistical mechanics or information theory that avoids signed-measure
   Jensen and controls an actual missing dependence term. Match every source
   hypothesis to the spatial DPP and the high-contrast parameter regime.

Useful exact bookkeeping to derive/check: with a product of disjoint R-site
block marginals as reference, KL(p_n||w_(a,R)) equals the sum of block entropies
minus H_n. For n divisible by R, stationarity gives (n/R)H_R-H_n. The supplied
value tail ALREADY makes this loss small per site as R grows; re-proving that
value estimate is not new. Its a-curvature or a signed Jensen error is unpaid.
An overlapping-block/finite-memory reference has additional boundary terms;
derive them, do not borrow the disjoint-block identity without checking.

A particularly useful scoped theorem would prove a uniform response/error
bound such as a fully quantified control of (1/n)partial_a^2 KL(p_n||w_(a,R))
on a declared compact interior a-strip at FIXED c>37/40, tending to zero as
R grows, OR a comparable mixed q/a derivative bound for a proved real-q
approximation. These are CANDIDATE interfaces, not supplied true statements.
Even such a derivative approximation does not give a sign automatically:
identify the additional finite/block sign obligation honestly. Alternatively,
a rigorously signed approximation plus an explicit o(n) Jensen error could
close the target. Explain exactly which conclusion you actually establish.

Explicitly track n, R, rho, c, distance from a-endpoints, and distance of q
from 1; declare the order of limits. A small-c expansion entirely inside
c<=37/40, a neighborhood shrinking to one a-point as n grows, or a q=2 result
does not resolve the live gap. Such calculations may be probes but not the
claimed new high-contrast advance. If a natural correlated-reference or
cluster estimate fails, give the exact still-live statement and an exact
counterexample/quantitative obstruction; do not generalize its failure to all
correlated approximations. Preserve the extensive dependence rather than
assuming measurement loss or omitted correlations are sublinear.


