# S4 round 4 — pay for the information curvature lost under localization

## Before working

This is the NEXT assignment after the already-dispatched round-three task.
Start it only after that task has finished. Do not interrupt or duplicate a
running attempt. Read [TARGET.md](TARGET.md), [KNOWN_RESULTS.md](KNOWN_RESULTS.md),
[PITFALLS.md](PITFALLS.md), and [CONTRACT.md](CONTRACT.md); all are mandatory.
Use [READING_MAP.md](READING_MAP.md) to select optional proofs and exact fixtures.

The task below continues your lane; it does not ask you to repeat the last
assignment. First compare the most recent result in THIS conversation with
the supplied reviewed baseline. In `DELTA_FROM_LAST_ROUND.md`, give its exact
claim, which parts are merely author claims versus independently reviewed,
the obstruction it resolved, and one genuinely unpaid next estimate. If no
new local result is available, use the unpaid gates below. If the main gate
below was just solved, move to the next uncovered growing-family/parameter
case or the quantified missing-error step; do not re-prove it for credit.
Preserve the main target. A new auxiliary candidate must be stated and tested
explicitly, not presented as known. Never treat an unavailable result from a
different task as a premise. Record the snapshot/commit you actually read.


Your specific task is to find a corrected localization/information-theoretic
method that survives your reviewed counterexamples. The key opportunity is
that the positive localization excess coexists with strongly negative total
entropy curvature: the omitted term need not be nonpositive, but must be paid
for. Do not repeat the tangent-only Gaussian or sparse-pinning counterexamples.

Use Gaussian observations or a precisely specified sparse experiment as your
starting object. For Z generated from X independently of the channel randomness,
with the law of (X,Z) independent of a, the exact identity is
H(Y_a)=E_Z H(Y_a|Z)+I(Y_a;Z).
Its second derivative includes the curvature of the mutual information, not
merely a posterior first-derivative energy. For Z_t=tX+B_t, a possible interface
to derive carefully is the conditional I-MMSE identity expressing
d_t I(Y_a;Z_t) as one half of the difference between the two MMSEs for X given
Z_t and given (Y_a,Z_t). Derive all conventions, factors, and mixed derivatives
instead of taking a sign from the name I-MMSE. The posterior given Z_t alone
does not depend on a, while the posterior given (Y_a,Z_t) and its averaging law
do. At a=(1-c)/2, zero first a-tangents do not kill second derivatives.

Scout these genuinely distinct payment mechanisms within this lane:
1. A score/conditional-variance identity retaining the unnormalized likelihood,
   second posterior response, and actual output Fisher information, seeking a
   bound on the POSITIVE information-curvature correction by a separately
   quantified negative budget in the conditional entropies.
2. A spatially structured localization or reveal schedule, with a signed
   correction summed before estimation, whose error can be bounded using the
   true Fourier/Toeplitz correlations rather than a worst-case posterior bound.
3. A relevant information-estimation or Bayesian experimental-design inequality
   from an adjacent field, but only after matching its exact channel and
   parameter direction; an SNR monotonicity theorem is not common-offset
   concavity without a proved conversion.
These are research seeds, not claims that any required bound is true.

First falsification gates: your old four-site block at c=.95,a=.025; nonuniform
rank-one input; and true contiguous P_(n,k) with k/n bounded away from 0 and 1.
These are diagnostics, not a requested re-certification of old results. Any
new proposed inequality must explicitly retain the term responsible for the
known positive excess. If it is only H''<=0 rewritten as I''<=-E H_cond'', label
it EQUIVALENT_BLOCKER and find a separately estimable term. A theorem for
arbitrary homogeneous laws is not granted and must face non-DPP tests.

A strong scoped deliverable would be a new uniform estimate on a specified
localization correction for growing contiguous projections at fixed high c,
with explicit rank and time dependence, or a rigorously paid Jensen defect
o(n). A quantified theorem valid on an explicit parameter subregion is useful.
A second block-product obstruction is worthwhile only if it kills a genuinely
different, precisely stated corrected payment that survived all supplied tests;
just tensorizing your old witness does not qualify. State exactly whether your
result depends on projection-minor compatibility and whether it transfers to
Q_n. No rare-branch supremum may silently replace the actual averaged law.


