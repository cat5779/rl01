# S5 interrupted round-four checkpoint — UNREVIEWED LEADS

The user supplied the following mathematical progress statements from a run
that stalled. There is no recovered full round-four proof or code package.
These are leads to reconstruct, not accepted premises or verified certificates.

- The author recognized that only the earlier actual-path identity was reviewed;
  the round-three mean-overlap surrogate approximation was not independently audited.
- The author reported an exact cancellation of the Fourier interaction potential
  after weighting changing conditional-slice expectations by the actual count law.
- A reference-normalization correction allegedly has curvature
  -2 n log n + O(n), and therefore cannot simply be dropped.
- The author reported a full-atom KL cost O(log n) from a surrogate law to the
  actual law at fixed density, but explicitly had no entropy-curvature control.
- Coupled six- and eight-site checks allegedly passed, including the Fisher term
  and both derivatives of the count-layer weights. No receipts were recovered.

The exact interaction potential, reference normalization, constants, parameter
domain, KL orientation, and all derivatives must be specified and proved before
these claims can be used. Do not invent formulas and attribute them to S5.

The recovered earlier [round-three author summary](S5_ROUND3_AUTHOR_SUMMARY.md)
provides one explicit mean-overlap BL surrogate and the claimed remaining term
W_n=sum_l[-2 pi_l' s_l Jtilde_l + pi_l'' Ftilde_l]. It is also unreviewed.
Its claimed o(n) entropy-value approximation may be a useful route to finite
Jensen transfer if reconstructed correctly; it is not pointwise Hessian control.
