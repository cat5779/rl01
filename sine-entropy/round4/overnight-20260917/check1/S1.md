# S1: resume global posterior compensation beyond bounded odds

Read the reviewed target, known results, pitfalls and reading map at https://raw.githubusercontent.com/cat5779/rl01/9032d04fbde2ae97ba9de2702ddcd07aa3923e89/sine-entropy/round4/ . The strengthened contract is overnight-20260917/CONTRACT.md at that commit. These files are public; private-repository access is not required.

The full fixed-density sine COMPLETE-configuration entropy-rate concavity target for 37/40<c<1 remains OPEN. Toeplitz Q_n is a contraction, not a projection. Any finite-cyclic surrogate needs a stated value/Jensen bridge to the true target. Preserve moving-law, acceleration, Fisher, normalization and count-weight derivatives. Never differentiate an uncontrolled value error or replace configuration entropy by spectral/count entropy.

This is a fresh research round: at least 90 minutes of substantive work unless a NEW fully established substantial theorem or rigorous obstruction to a live, consequential mechanism is obtained. No time padding, false time reports, tiny epsilon improvements, repeated finite tests, or 'if P2 then the target' as completion. Honest INCOMPLETE is acceptable. Keep checkpoints. Return one cumulative self-contained ZIP (MD/PY split allowed), but ALSO put exact statements, key definitions and derivation, unresolved steps, constants/scaling and a next research obligation directly in the chat. The coordinator cannot download your ZIP during this interval. All new author results remain unreviewed until an actual independent audit. Preserve old artifacts. No PR.

Your previous reply derived an exact common-posterior expression for pairwise curvature and a Schur-power compensation bound only when every normalized posterior odds variance alpha_i=w_i/u_i^2 is below one. It left all-dimensional projection compensation open. Treat your own unreviewed statements as checkpoints to reconstruct where needed; do not count old identities, small dimensions or projection-value bridges as new results.

For a complete output y, sigma_i=2y_i-1, posterior kernel R^y, likelihood ell_i(x)=Pr(Y_i=y_i|X_i=x), kappa_i=c/[ell_i(0)ell_i(1)], u_i=p(y^i)/p(y), w_i=kappa_i^2 R_ii(1-R_ii), tau_ij=kappa_i kappa_j|R_ij|^2, and x_ij=tau_ij/(u_i u_j). Retain the SAME posterior for all pairs. Your cell functions were F_+(x)=-2[x/(1-x)+log(1-x)] on 0<=x<1 and F_-(x)=2[log(1+x)-x/(1+x)] on x>=0. Odd-parity x need not be <1. Do not use a divergent power series there.

New objective: overcome the alpha<1 restriction by a convergent integral/resolvent representation, a controlled resummation, or an exact good/bad-output decomposition that retains actual-law weights and the negative nonlinear remainder. For projection input the posterior is again a projection and sum_{j!=i}|R_ij|^2=R_ii(1-R_ii); exploit the common structure rather than optimizing independent pair tables.

First freeze a specific lemma that bounds the summed large-odds contribution or extends the Schur-power comparison. Establish its mathematical hypotheses and test it on genuine contiguous Fourier posteriors and the accepted completion-(C) counterexamples. A rare event estimate must pay the size of its integrand, not only its probability. Discarding all negative pair contributions may destroy precisely the compensation needed.

Seek either a new dimension-uniform signed estimate on a fixed positive-density family (start with half density and a declared high-contrast compact strip), or a rigorous obstruction to a precisely stated stronger common-posterior comparison. Do not claim the full target from a lemma equivalent to its Hessian sign. If a candidate is false, preserve the exact compatible counterexample and advance a genuinely different paid variant within the research period.

Report how any new constants interact with the full diagonal Fisher budget, and whether bounds remain useful as n grows. A conditional theorem assuming the unpaid global budget itself is not a result. Start now.
