# Hypothesis, gap, and nonclaim audit

**Author audit, not independent review.** Status PROVED_SCOPED_LEMMA concerns
the stated analytical spatial estimates. The ultimate target remains open.

## Object and probability law

Q_B is T_B(1_E), a finite positive contraction, not a finite projection.
The finite variance identity retains (R-R^2)_ii. Only the ambient Fourier row
identity after zero extension of a finitely supported test function uses a
projection. This accounts for the boundary explicitly.

The entropy is -sum_y p_y log p_y over every labelled binary word. Both occupied
and empty sites enter the signed determinant. Neither count entropy, trace
binary entropy, an integer Renyi entropy, nor a unitarily conjugated atom law
is substituted. The only gauge used to center the frequency interval is a
diagonal phase gauge, which preserves atoms.

The posterior external-field normalizer is the actual p_y. Its full-word
average is never replaced by a uniform-word or cardinality-layer average.
Theorem F uses the actual outside marginal twice, coupled conditionally on the
local observations. Its forced all-zero/all-one completion costs m^(-k),
k=1 or 2; this change of measure is proved in (2.7), not silently discarded.
Probability tables obey the tower rule. Conditional kernels are NOT claimed
to obey it or to form arbitrary linear mixtures with the same kernel.

## Derivatives and limit passage

The finite identity is H''=-sum_i E f(u_i)+2sum_(i<j)E g_ij. In each mixed
coordinate derivative the appropriate outside marginal is fixed because it
is independent of those TWO shifts. Summing every coordinate partial yields
the full common-shift derivative. The acceleration Lambda and mixed Fisher
-s Lambda' are retained together. There is no differentiation while freezing
the common-shift output law, and no missing count/reference derivative.

The independent Bernoulli reference in the integrable determinant formula
is accompanied by its full expected log determinant, which can be extensive.
A reference entropy is not treated as the full entropy.

Theorem F first compares actual finite Hessians to the same finite local
conditional expression and proves uniform Cauchy convergence on strict compact
shift intervals. It then integrates twice and uses value convergence to fix
the affine constants. It does not infer derivative convergence from value
convergence alone. Existence of the entropy rate and basic C^2 regularity are
not claimed as novel baseline improvements.

## Quantifiers and constants

Theorems A--C and F hold for all finite n>=1, all 0<rho<1, fixed 0<c<1,
and 0<a<1-c. Every cutoff is a positive integer. R>n yields a vacuous far
sum; absent distances and failed local windows in Theorem F are explicitly
paid even when R or L exceeds n. Ordered versus unordered sums and the
factor two in mixed curvature are recorded in each statement.

The constants B_*, C_*, D_*, the completion factor m^(-k), and the fallback
weighted exponent alpha=delta/128 are explicit. None is endpoint uniform.
The stronger posterior-energy constant 3/2 is noise independent; its transfer
to G or g is not. The candidate's earlier 7/4 was improved analytically and
is preserved only in the dated prospective record.

The n/R lower bound concerns posterior squared-entry energy, not entropy
curvature. It holds at the fixed density 1/2 and fixed strict noise as n and R
grow. The alternating-word inverse obstruction likewise keeps density and
noise fixed while the actual Toeplitz volume grows. It is not a fixed-rank or
artificial finite-projection example. Its positive-probability words can be
rare; it does not refute a probability-weighted estimate.

## Certificates and their scope

The six-site witness enumerates ALL 64 atoms and ALL 16 outside words for the
chosen pair. Half-density sine entries reduce to polynomials with rational
coefficients in t=(c/pi)^2. Pi is enclosed with the Machin identity and an
alternating-series remainder; logarithms use a rational atanh series with an
explicit remainder and outward rounding. The asserted rational inequalities
are checked directly, not inferred from printed decimals.

The separate 1004 identity checks and 8049 localization checks use a rational
positive-contraction fixture and exact Fraction arithmetic. They audit finite
algebra and both fixed-completion choices; they are not proofs over all rho,
n, or channel parameters. The analytical arguments supply those quantifiers.
Normal and python -O runs agree after removing diagnostic elapsed times.
Neither a second script nor an optimized replay is independent peer review.
The preliminary 48-case floating probe is exploratory only and is not a
certificate or a thermodynamic inference.

## External hypotheses and novelty boundary

SCOUT_LEDGER records three initial mechanisms and two promoted proof attempts.
The Janossy source includes a discrete projection model; it is not dismissed
as purely continuous. Polynomial differential/Pearson closure hypotheses do
not automatically survive arbitrary binary fields. The exact growing-degree
obstruction excludes only the named fixed-degree interface. The inverse-decay
literature requiring s>dimension is not applied at the sine kernel's s=1.
No published de Branges theorem, general Riemann--Hilbert method, or conditional
ensemble theorem is claimed disproved.

The existing pencil gap, common-posterior identities, scalar g identity,
midpoint Fisher formula, and response bounds are recognized as prior work.
The new control is the actual-law spatial energy, its full-Hessian bridge,
its conditioning-radius payment, and the explicit growing-word obstruction.
No exhaustive external priority claim is made.

## Remaining estimate

To obtain the requested high-contrast concavity from Theorem F, one must prove
an upper bound on the actual local sum K_(R,L) strong enough that

    K_(R,L) + C_*/R
      + 156(R-1) B_* delta^(-4) m^(-2)/L <= 0

using the one-sided refinement in ONE_SIDED_CURVATURE.md, on an appropriate
growing-cutoff exhaustion of the strict legal shift domain,
or supply a comparable integrated signed estimate. This sign is not proved.
The averaged-pair-positive sine certificate blocks the stronger shortcut
E g_ij<=0. Reflection exchanges the pair variables but leaves g symmetric,
so reflection alone does not cancel positive contributions.

The endpoint problem is not paid by these divergent constants. A complete
proof of the frozen full legal interval is therefore NOT supplied. No tiny
contrast extension is offered in place of it.
