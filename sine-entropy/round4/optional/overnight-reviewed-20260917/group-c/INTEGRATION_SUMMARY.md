# Group C integration summary

## S7 / PR111 — ACCEPTED_SCOPED / PARTIAL_OVERALL

Integrate `research/s7-round3-integrable-operators-20260917/`.

- **ACCEPTED:** for every finite `n`, `0<rho<1`, strict
  `0<c<1`, `0<a<1-c`, and positive `R`, the complete labelled-Shannon Hessian
  identity, absolute far-pair curvature bound `n C(a,c)/R`, actual-posterior
  energy bound `3n/(2R)`, and the stated word-uniform/actual-law inverse bounds.
- **ACCEPTED:** for every positive `n,R,L`, Theorem F's genuinely finite
  conditional-table enclosure with errors `C_*/R`, the displayed
  `O_(a,c)(R/L)` observation cost, and the finite-volume boundary term;
  Corollary G and the strict-channel word-uniform weighted locality result.
- **ACCEPTED:** the analytic alternating-word obstruction to uniform `C/r` and
  fixed-polylogarithmic-over-`r` inverse bounds.
- **PARTIAL:** near-field sign, endpoint-uniform control, improved contrast
  threshold, and full sine entropy-rate concavity remain open.
- **CRITICAL GAP (certificate unit):** PR111 omits the scripts and generated
  payloads named by its reproduction guide and receipt. Do not cite the
  six-site rational inequalities or 1004/8049 check totals as independently
  reproduced. The receipt is author provenance only. This does not reject the
  self-contained analytic spatial theorems.
- Independent audit: `research/s7-round3-integrable-operators-20260917/INDEPENDENT_AUDIT.md`.

## S9 / PR113 — ACCEPTED_SCOPED / PARTIAL_OVERALL

Integrate `research/s9-round3-signed-compensation-20260917/`.

- **ACCEPTED:** at half density, fixed `0<c<1`, odd aligned `R -> infinity`,
  the production-weighted Gaussian-mixture limit and symmetrized signed
  compensation, uniformly on fixed compact critical-shift scales.
- **ACCEPTED:** band/complement midpoint curvatures
  `-R C_b(c)+o(R)` and `+R C_b(c)+o(R)`; at `c=19/20`, `b=1/2`,
  `C_b(c)>1/140`. The full leading curvature is only `o(R)`.
- **ACCEPTED:** finite full-law score/acceleration and KL theorems, the
  noise-adapted strict-interior fixed-density extension, and the two scoped
  surrogate-route obstructions.
- **PARTIAL / CRITICAL GAP for the target:** the complete subleading signed
  aggregate remains unsigned. Existing transfer/CLT errors do not pay the
  critical `1/R` Jensen scale. No eventual numerical radius, fixed-chord sign,
  contrast extension, or sine-target proof is obtained.
- Verification: exact 16-atom and rational checks independently replayed with
  Python 3.13.5 / SymPy 1.14.0 and passed. Floating probes are diagnostics only.
- Independent audit: `research/s9-round3-signed-compensation-20260917/INDEPENDENT_AUDIT.md`.

## S3 — MISSING

No new 103-file S3 cumulative package was present. The older
`S3_completion_followup.zip` was not re-reviewed.

## Root integration requirements

- Preserve the frozen source heads and exact scoped quantifiers above.
- Keep `ACCEPTED`, `PARTIAL`, and `CRITICAL GAP` statements separate.
- Add every staged S7/S9 file to the repository-level manifest. PR113's root
  manifest does not cover all new files, and PR111's receipt references absent
  verification files; do not propagate either condition silently.
- Do not promote S7 localization or S9 leading compensation to a sign theorem
  for the complete entropy target.

