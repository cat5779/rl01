# Runtime output directory

Replay and diagnostic scripts write here by default, or to an explicitly chosen fresh directory. This directory is not frozen evidence and is excluded from the shipped mathematical hash manifest. Never copy new output over `evidence/` merely to force a replay to agree.
