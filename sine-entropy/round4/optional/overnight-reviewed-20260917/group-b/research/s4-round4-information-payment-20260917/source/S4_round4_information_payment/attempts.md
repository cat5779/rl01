# Attempts, rejected shortcuts, and retained versions

## Attempt A — not promoted

Starting from the old exact local expression for Q_pi'', keep the full unnormalized second derivative and try to reduce the delta^-5 local coefficient. The obstacle was the extrinsic-pair to full-posterior inverse-likelihood cost. No new local-in-time inequality was proved. The method was not relabeled as successful after moving to a different functional.

## Attempt B1 — promoted, completed

The initial frozen claim is in `notes/scout_and_freeze.md`. The next explicit factor-two and one-percent regional statement is in `notes/B1_constant_and_region_frozen.md`. The channel-only kappa extension is in `notes/B1_general_constant_frozen.md`.

The decisive sequence was:

1. Retain complete channel scores and all output probability jets. Derive I''=A+E and E=Psi-2S.
2. Regroup the fully differentiated A term into actual-average conditional output log odds, without differentiating a subsequently frozen extrinsic reference.
3. Prove a scalar four-cell estimate using the equal-output probability enforced by the actual channel.
4. Rebase each extrinsic determinant to the same full posterior with exact likelihood factors, before summing edges.
5. Combine A and E with their signs intact, obtaining I''<=kappa Psi.
6. Compare Psi with the scalar missing-Fisher budgets, then prove a uniform one-percent trace region.
7. Apply the same theorem to Gaussian posteriors and bound the completed remainder by Gaussian affinity, with deterministic stopping and explicit rank/time dependence.

Each connecting step is written in `proof.md`. No step is only the target written as I''<=-E H_cond''.

## Failed constant-one simplification

The suggested simplification of B1 to I''<=Psi is false at the required skew rank-one gate, with two input atoms of weights 1/100 and 99/100, c=19/20, a=1/40. Exact rational log enclosures give positive failure; the displayed ratio is only a rounded diagnostic. The factor-two statement was not inferred from a scan: its separate scalar proof controls all positive four-cell laws with p00+p11>=1/25.

This failure is not offered as a new deliverable or a new obstruction to a surviving global method. It simply prevents an unjustified constant improvement and is preserved in the certificate's `factor_one_fails_at_this_fixture` field.

## Attempt C — not promoted

A spatial reveal/segmentation idea could potentially exploit true Fourier cross-interface correlations. However, neither an actual signed curvature sum nor a uniformly paid o(n) full-target Jensen error was derived. A value estimate alone would not meet the task. The Gaussian truncation remainder proved in B1 is a different, precisely specified object and is not presented as success for C.

## Non-DPP gate

A rank-two homogeneous input supported on 11000 and 00110, each with probability 1/2, has Cov(X_0,X_1)=1/4>0. It fails a necessary structural hypothesis. The checker does not feed it into the DPP proof as if homogeneity sufficed. Its additional floating channel calculations are not a new counterexample or theorem.

## Practical failures and repairs, not research credit

Archive content was not indexed by the Files search. The visibly mounted preceding ZIP was inspected with the container instead, with no private-source search. Direct container HTTP copying failed DNS; the mandatory packet and primary pages were nevertheless read through the available public web tool. The package contains a truthful reading record, not fabricated source-file byte hashes.

The exact arithmetic helpers adapt earlier infrastructure with attribution. Replay uses UTF-8/LF writes and parsed JSON, and deliberately tests CRLF; this is a portability repair, not new mathematics. No initial exact or moving-reference check failed after the final mathematical statements were frozen. All actual rejection evidence is retained rather than manufactured for the ledger.

A final packaging pass initially used the wrong start-record key and halted before creating the final ZIP. `notes/packaging_repair.md` records the repair and the intermediate stale-manifest check. No mathematical evidence or original clock record was altered.
