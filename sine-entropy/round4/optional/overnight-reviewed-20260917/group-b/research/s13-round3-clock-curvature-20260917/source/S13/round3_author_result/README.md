INCOMPLETE_TARGET__PROVED_SIGNED_COMPONENT__AUTHOR_UNREVIEWED

# S13 continuation — round three

## New result

For the genuine half-density contiguous cyclic Fourier family, every fixed
`0<c<1`, and the symmetric legal shift `a_*=(1-c)/2`, `proof.md` isolates the
clock-curvature entropy-production component

`C_n(c)=sum_l pi_l(a_*,c)[-tauhat_l''(a_*)] I_l(tauhat_l(a_*))`

inside the corrected-law uniform-slice divergence response and proves

`liminf_(n even -> infinity) C_n(c)/n >= 2 D_pair(c)/(1-c^2)>0`,

where

`D_pair(c)=2[(1/4-c^2/pi^2) log(1-4c^2/pi^2)
             +(1/4+c^2/pi^2) log(1+4c^2/pi^2)]`.

At `c=19/20`, the explicit lower coefficient is
`1.4045396549292533...`.

The proof retains the actual count law, derives the full chain rule before
bounding, proves the clock-curvature limit uniformly on central growing layers,
uses a nonzero averaged Johnson degree-two adjacent-pair mode to force extensive
slice KL, reconstructs the slice modified log-Sobolev normalization, and pays
the count tails.

## What this does not prove

The moving-count term

`W_n(c)=sum_l pi_l''(a_*,c) K_l(tauhat_l(a_*))`

is not signed or bounded sharply. Numerical diagnostics suggest it may contain
a larger adverse middle-layer cusp; that observation is not a theorem. Away
from the midpoint, the time-Hessian term involving `(tauhat_l')^2 I_l'` also
remains unpaid. No sign is claimed for the full corrected response, the actual
cyclic entropy, the Toeplitz sine target, or the entropy-rate limit.

## Review status

A fresh external reviewer checked only the bounded algebraic gate supplied by
the user: the exact theta formulas, `0<theta<1`, and layerwise matching of the
one Fourier pair-potential expectation by the positive BL clock. The theorem in
this directory, including the saddle estimate, mLSI application, and extensive
constant, is an author result awaiting independent audit.

The previous random-forward-clock two-mode no-go is preserved only in its
proper conditional/operator scope. The averaged initial law has zero Johnson
degree-one component. This round makes no averaged-law degree-one no-go claim;
it explicitly uses a nonzero averaged degree-two mode instead.

## Deterministic replay

From this directory:

```bash
python scripts/replay.py
```

The replay regenerates all frozen JSON receipts in a temporary directory and
requires byte identity. Floating receipts are diagnostics, not proofs.

## Files

- `proof.md` — self-contained theorem and proof.
- `TERM_BALANCE.md` — exact response decomposition and sign ledger.
- `RECOVERY_LEDGER.md` — reviewed, reviewer-checked, unreviewed, and new items.
- `gap_audit.md` — paid and unpaid obligations.
- `attempts.md` — retained failures and numerical warning about `W_n`.
- `candidate_ledger.md` — candidate promotion record.
- `SOURCE_LEDGER.md` — pinned source provenance and literature provenance.
- `scripts/` — deterministic symbolic, finite, and asymptotic checks.
- `evidence/frozen/` — immutable receipts generated by those scripts.
