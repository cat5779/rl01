# Group A integration summary

The following lines are intended for direct use in repository-level ledgers.

- **S1 — PARTIAL.** Accept the standalone weighted rare-word theorem for even
  `n`, the contiguous rank-`n/2` Fourier projection, `c=19/20`, every
  `a in [1/50,3/100]`, and every `u>0`: the actual outside-count tail obeys the
  stated Bernstein bound and the submitted `W_i`, bad-star, signed-integrand,
  and absolute-integrand tail contributions obey (7.3)--(7.7), hence are
  `O(n^(1-gamma))` under the explicit `u_(n,gamma)` choice.  Do not promote the
  imported check-1 all-odds identity or the resulting claim that the central
  band is the sole remaining entropy-curvature sector; those dependencies were
  outside this new-claim audit.  The central band remains unsigned and no
  `H_n''<=o(n)` or sine-rate concavity follows.  Audit:
  `research/overnight-reviewed-20260917/group-a/s1/INDEPENDENT_AUDIT.md`.

- **S2 — ACCEPTED (scoped).** For every odd `n` and `3<=k<=n-4`, with the
  explicit reflection pairs in the proof and every `t,u in (0,1)`, the first
  affine transposition average of the consecutive Fourier rank-`k` law is a
  projection DPP law, whereas the twice-averaged law is not any
  Hermitian-contraction DPP law.  Also accept the exact `Pi_(7,3)` certificate
  at `(a,c)=(1/200,19/20)`: first physical gain in
  `[2.234011527651,2.234011527652]`, second physical gain in
  `[-0.888244305490,-0.888244305489]`, ideal second affine gain in
  `[0.860920037700,0.860920037701]`, and actual-minus-affine correction in
  `[-1.749164343190,-1.749164343189]`.  The block tensorization is extensive
  only for a direct-sum density-`3/7` family, not the growing contiguous Fourier
  family.  General projection concavity and the sine target remain open.
  Audit: `research/overnight-reviewed-20260917/group-a/s2/INDEPENDENT_AUDIT.md`.

- **S11 — ACCEPTED (scoped).** Accept the exact obstruction to the declared
  differentiated split-tail certificate family: even after the stated best-case
  relaxations, every legal profile has payment greater than
  `(366368000000/1323)(c-d)`, giving strong-margin and sign-only total-width
  ceilings `3969/73273600000000` and `1323/18318400000000`.  This is conditional
  on choosing that certificate architecture and does not certify its imported
  rejected-checkpoint tail lemma.  Independently accept the low-mode identities
  and the `n=2, Q=diag(1,0)` witness: mode two costs at least
  `(4177920000/47458321)(c-d)` over every legal shift in the benchmark slab,
  while the complete centered entropy response from `0.925` to `0.926` is
  `-3125000/8800857`.  This rejects nonnegative mode-by-mode bookkeeping; signed
  grouping and base-margin coupling remain available.  Audit:
  `research/overnight-reviewed-20260917/group-a/s11/INDEPENDENT_AUDIT.md`.

- **S12 — ACCEPTED (scoped).** Accept the complete-law coordinate-bias Hessian
  formula and the explicit benchmark witness at `c=463/500`, `a=37/1000`:
  `H_12=log(4/3)-29/102>1/714`; direct sums give
  `2 sum_(i<j)(H_ij)_+>n/714`.  Accept also the scalar product witness
  `Q=(1/100)I`, for which `(1/n) partial_c partial_a^2 H>560/169` over the full
  centered strip of half-width `1/200`.  These refute a facewise mixed-term sign
  and a free nonpositive contrast-response sign.  Their total shift curvatures
  remain negative, so they are method counterexamples only.  Total-correlation
  convexity and the sine target remain open.  Audit:
  `research/overnight-reviewed-20260917/group-a/s12/INDEPENDENT_AUDIT.md`.

- **CRITICAL GAPS (group-wide).** No packet proves the central-band sign,
  general projection inequality, total-correlation convexity, a paid path for
  growing contiguous Fourier projections, or the fixed-density sine
  complete-configuration entropy-concavity theorem.  No packet disproves that
  theorem.  The target status therefore stays **OPEN**.

