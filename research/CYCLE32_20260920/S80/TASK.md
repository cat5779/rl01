# S80 — An actual-law, scale-stable Fisher–Burg payment

## Research in this chat
You must NOT route, redirect or hand off the user to Work, another chat, another agent or a cloud work task. This is a mathematical derivation task in THIS chat. A checkout is not a prerequisite. If export fails, return complete readable mathematics here.
Think for at least 120 minutes, unless you achieve major progress earlier. Do not invent duration or idle to fill time. Major progress is a proved useful tool, a decisive model-valid obstruction with a useful replacement, or a closed theorem.

## Frozen target and ownership
The ultimate target is concavity in a of the full spatial-configuration Shannon entropy rate for K=aI+c Q_rho on Z, where Q_rho(i,i)=rho and Q_rho(i,j)=sin(pi rho(i-j))/(pi(i-j)), for fixed 0<rho,c<1 and all 0<a<1-c, particularly the entire contrast interval (37/40,1). A finite Toeplitz compression is a strict contraction, not a projection; count entropy and trace binary entropy are different objects.

You inherit S77's independently verified visible core (RL01 PR67), not its truncated final section. The new obligation is a quantitative, scale-stable estimate for the actual Toeplitz Burg charge. Do not re-prove the exact identity as your main result. This route works directly with actual finite blocks. S78's pair-barrier tool and S79's shell formulas are neighboring results; this task is not a repeat of those derivations.

Aim for fixed rho and any fixed c<1 on compact legal bias intervals, allowing explicit gap dependence. A first useful milestone may be half density on a stated nontrivial parameter interval above37/40, but distinguish it from the ultimate target.

## Audited input: actual-law Fisher–Burg identity
For adjacent finite blocks A,B, let p(y)=P_K(y), q(y)=p_A(y_A)p_B(y_B), M=H_A+H_B-H_AB=D(p||q), with derivatives along the common scalar shift a. All words are positive on a compact interval J with delta I<=K<=(1-delta)I.

Write g=p/q, ell=log g, sigma_i=2y_i-1. Deleted-coordinate marginals are p_-I,q_-I. For i<j put m_ij(y_-ij)=p_-ij/q_-ij, and let Delta_ij f=f11+f00-f10-f01. Define
Phi(x,m)=2[x-m-m log(x/m)],
I_rel=E_p[(partial_a ell)^2],
B=sum_(i<j) sum_(y_-ij) q_-ij Delta_ij Phi(g,m_ij),
Bplus=sum_(i<j) sum_(y_-ij) q_-ij [Delta_ij Phi(g,m_ij)]_+.
All pairs, including within-block pairs, and every actual word are included.

PR67 independently verifies
M''=I_rel-B,
D_F=I_rel-2E_p[S_A S_B],
C_acc=2E_p[S_A S_B]-B,
where S_A,S_B are actual marginal scores and E_p[S_A S_B]<=0.
Thus a large previously separated Fisher/acceleration contribution cancels EXACTLY.
M''>=I_rel-Bplus is valid, but Bplus<=I_rel is a STRONGER sufficient condition; B<=I_rel is exactly equivalent to M''>=0. Neither is proved generally.

Available derivation tools:
p'=sum_i sigma_i p_-i,
p''=2sum_(i<j) sigma_i sigma_j p_-ij.
The same linear cofactor equations hold for q and h_t=(1-t)q+tp. This auxiliary probability bridge is generally NOT a DPP; negative association must not be applied to it.
For z_t=(p-q)/h_t, the verified Bochner identity is
partial_a^2 sum_y (p-q)^2/h_t
=2E_(h_t)[(partial_a z_t)^2]
 -2sum_(i<j) E_(h_t,-ij)[Delta_ij(z_t-z_t,-ij)^2].
Integrating with weight1-t produces the exact identity above.

## What must be created now
Construct a reusable functional or multiscale comparison that bounds the dangerous actual-weight Burg contribution after retaining its Fisher payment. Explore an appropriate transferable mechanism, such as a weighted discrete Bochner/curvature estimate, conditional entropy production, a martingale decomposition, or a scale recursion. Specify inputs, hypotheses, output and the legal interface. Verify borrowed literature hypotheses against the actual law; the name of an inequality is not a proof.

A candidate output is Bplus<=theta I_rel+E_cut with theta<=1 and an explicitly summable, affordable E_cut. This is NOT assumed true. If taking positive parts destroys essential cancellation, keep a specified signed grouping of rectangles instead and prove its residual bound. Do not merely replace one unknown by another, or estimate all resolvents wordwise by delta^-1. Prove a strictly useful intermediate estimate even if the final sign stays open, and calculate its actual loss.

First attack overly broad versions before committing to them. A failure for a generic contraction only rejects that generalization; a counterexample relevant to the sine target must use a legal actual sine compression and actual weights. Do not discard rare words, replace conditional laws by iid surrogates, or infer all-size signs from finite scans.

## Exact rate interface and scale accounting
Let F_N=-H_N''. An adjacent merge obeys F_AB=F_A+F_B+M''.
For equal adjacent blocks of length m define a rigorously uniform residual e_m(J)>=sup_(a in J)[Bplus-I_rel]_+, or a better residual from your proved signed comparison.

For a seed F_L>=f_L throughout J and dyadic merge levels through K, S74/PR64 and S77/PR67 give the sufficient entropy-rate curvature margin
kappa >= f_L/L
 -sum_(k=1..K) e_(2^(k-1)L)(J)/(2^k L)
 -B_delta c^2 D_(rho,2^K L)/(2^(K+1)L),
where D_(rho,n)=tr(Q_n-Q_n^2).
One valid conservative inherited B_delta is6 delta^-12. Use sharper audited constants only if you state them correctly. The proof integrates finite-volume curvature inequalities before taking entropy-value limits; it does not differentiate an o(N) error.
Alternatively, an all-scale residual with affordable weighted sum can avoid that coarse tail. Report seed, interval uniformity, scale coverage and exact residual. Do not pick an astronomically large K while silently assuming every intermediate scale is paid.

At rho=.5,c=.95,a=.025, the verified formula evaluated in double precision gives N6: I_rel about18.55254643, Bplus about6.18892663, I_rel-Bplus about12.36361979. N2 through12 and a small45-point scan were positive. These are FLOAT DIAGNOSTICS, not interval certificates or an all-scale theorem. PR64 independently certified the DIFFERENT quantity M'' at the six-site point; do not inherit that certificate for the new Bplus expression.

## Established obstruction to avoid
For two equal marginals u=a+c rho and weak d=|K12|^2, S77 proved -C_acc/D_F tends to1 as d tends to0. Thus a fixed theta<1 with residual o(d) cannot hold uniformly even for strict two-site contractions. A quartic-in-K12 residual is O(d^2), so it cannot repair that relative estimate. The new Fisher–Burg cancellation is designed to expose the surviving d^2 scale. The verified two-site lower bound is
M'' >= [3-10u(1-u)] d^2/[u^4(1-u)^4] >0.
It cannot be summed over edges without a proved interaction estimate.

## Delivery
Return an English RESULT.md or complete in-chat proof, with PROVED/DISPROVED/INCOMPLETE separated, exact quantifiers, proof dependencies and remaining rate deficit. A mathematical tool with a genuinely paid partial obligation is valuable; a new identity alone is not a paid estimate. Computational work may guide and falsify; provide source and actual execution status, clearly distinguish rigorous interval results from floating point.
No SHA/checksum/hash acceptance gate. Do not create other agents, mutate shared repositories or merge PRs. Do not route to Work.

