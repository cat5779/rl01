> **待独立对抗性审查 / PENDING_INDEPENDENT_REVIEW。** 下文保留来源原文；其中 PROVED、PASS、AUDIT 等是作者或既有记录的表述，不代表本次 SA 成果已独立通过。先读本包根目录 REVIEW_REQUEST.md；外部审计与其他 SA 分开进行。

# SA03 最终复核记录

## 1. 交付范围

对应 PR #118 的 TASK_03_S9_SUBLEADING_RESIDUE.md。本次写入限于 `research_prompts/pro_tasks/results/SA03_S9/`，没有改写原任务、旧审核、其它任务文件或合并 PR。已成功写入指定分支，并重新读取结果索引、定量结论和 PR 元数据。旧 S9 审核不覆盖本次新定理。

本地完整包保留较展开的证明，PR 中为同一证明链的紧凑版本；式号和文字展开程度可能不同，不主张逐字相同。两版均包含模型、真实 word 身份、残差、尺度、量词和缺口，不需要运行原仓库的大型程序才能阅读证明。

**PROVED 表示本交付给出完整推导，尚未独立审查。** 小脚本只验证指定有限代数断言，不构成成长体积定理的计算认证或新颖性认证。

## 2. P1–P3 与证明依赖

P1：从真实质量导数构造 L、T 并支付 w'；二阶展开包含 T'、T²、交叉项和全部 u 因子。没有以计数独立模型代替物理 word。

P2：固定测试函数边界式为 E[F Gcal+2 Hcal LF+phi(q)L²F]。F=1 时运输逐 word 为零，补带通量取反，不依赖高斯近似或移动坐标。

P3：带中心响应权重的连通双翻位估计，加上 Bregman 差分，支付全维数求和，给出点态 |Gcal-8u²|<=C_cu⁶ 和完整 A_R''=O_c(1)。没有宣称裸双差分绝对和一致有界。

体积极限另付共同 Hilbert 空间的强收敛、全 word 一致列尾、双翻位尾、真实有限边缘变化和 u 积分。定量附录给显式整数截断误差，不把未知曲率之差改名。

## 3. 状态更新

结构补充原 S4 的复解析候选已由 `SA03_UNIFORM_WEAK_NOISE.md` W2–W4 完成。该证明用复对称双线性形式 v^T v，而非把复欧氏范数视为解析函数；逐项重做赔率逆、单跳跃、双翻位和复 Taylor 估计。主报告早期“小 u 余项未一致化”的状态据此更新。

小参数是 cu，准确术语是弱耦合/低有效对比度。早期“小噪声”字样应按公式理解为小 u，不能解读成物理信道噪声较小；u 接近 1 是尚未判号的高有效对比度端。

`SA03_FIXED_R_NEXT_TERM.md` 识别固定 R 的下一系数 (4096/3)sum_i|p_hat_R(i)|⁶，但没有把更高阶 O_R 余项宣称为 R 一致。系数收敛不等于 Gamma 已有六次展开。

## 4. 已执行的小算

测试环境 Python 3.13.5、SymPy 1.14.0。

```bash
python small_check_r1.py
python check_resolvent_algebra.py
python check_r1_refinement.py
```

三个结果 JSON 均 PASS。

第一脚本仅真实 R=1 的 8 个联合原子/4 个外部 word，核对归一化、后验、完整 jets、通量伴随、Dq=u、完整二阶抵消、任意固定测试数组边界式和精确曲率。

第二脚本仅两个相互作用外部位的通用 3x3 矩阵，核对翻位逆、Schur 能量、DG=0、Poisson 生成元分解、Dr_i=2u²o_i、Do_i=-2uG_ii，不替代实际 Fejer 检查。

第三脚本复用同四个 R=1 word，核对结构 S6 和逐 word 首项：00、11 为 8+80t²+O(t³)，01、10 为 8+16t²+O(t³)，t=c²u²/pi²。

另以单变量精确符号导数核对有理系数 384、4096/3。没有扩大到 R=3 的枚举，没有扫描、拟合、批量区间认证或旧证书重算。Markdown 数学环境与代码围栏配对已核对；没有生成或验证哈希清单。

## 5. 失效规则与最终缺口

真实 R=1 的 T Bcal_phi=-16u²t²/(1-4t²)<0，否定其非负耗散规则；同一模型完整曲率为正。这不是全局猜想的反例。

未证明 Gamma(19/20) 的符号、充分大 R 的完整曲率符号、尖锐体积误差和首个非零更细项、J_R 到真实熵率的二阶稳定或固定弦桥接。巨大绝对值常数、小 cu 正系数和有限 R 正性均不能代替这些有符号义务。

固定 R 更高阶系数已有，将其余项再次一致化仍是独立的小参数理论缺口，不影响已证 O(1)、Gamma 极限、有效 R 余项和统一四次首项。

## 6. 来源核查

已读取任务、COMMON_STARTER、S9_AUDIT，并核对 S9_SCORE 和 S9_MESOSCOPIC 的相关范围。外部来源为 Braverman 的 prelimit Poisson 方法、Chafai 的离散 Phi-calculus，核对了原论文相关页和书目信息。未借用队列模型 Stein 因子或正耗散作为 DPP 定理，未完成全面新颖性检索。

所有已证结论由报告中的有限身份和不等式推导。重型后续工作在 COMPUTATION_HANDOFF.md 明列为未执行。
