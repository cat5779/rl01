# S76 — Pay the exterior-channel acceleration using its overlap geometry

## Mathematical derivation here; routing to Work is prohibited

You must NOT attempt to route, redirect, hand off, or instruct the user to switch to Work, a cloud work task, another chat or another agent. Do the mathematical research in THIS chat. A repository checkout or external execution environment is not a prerequisite. If sources or exports fail, use the explicit definitions below, identify the missing dependency, and deliver complete readable mathematics in chat.

Think for at least 120 minutes, unless you achieve major progress earlier. Do not fabricate duration or idle to fill time. A proved useful structural bound, a decisive model-valid obstruction plus a useful replacement, or a theorem closing the stated scope counts; a survey, renamed equivalent remainder or a few favorable numbers does not.

## Scope and ownership

The ultimate target remains concavity in a of FULL spatial-configuration Shannon entropy rate for the true stationary sine DPP K=aI+cQ_rho, every fixed0<rho,c<1 and every0<a<1-c, especially the entire high-contrast interval(37/40,1). Finite Toeplitz compressions are contractions, not projections. Do not replace full Shannon entropy by count entropy or Tr h_b(K).

Your distinct task starts from S73's independently verified exterior-channel structure (RL01 PR62), incorporating its required interface correction. S74's new cut bound and S75's new star theorem are still undergoing separate audits and are not accepted premises here. Their manuscripts are intentionally not your input. Do not re-prove S73's representation or its four-site obstruction. Construct a new signed compensation estimate for the combined entropy, retaining the acceleration term. The first meaningful milestone may fix rho=1/2 and symmetric bias but should address arbitrary size and an explicit contrast interval, ideally every fixed37/40<c<1, not only one c=.95 numerical point.

## Exact finite model

For 2m cyclic sites let Q be the rank-m projection onto m consecutive discrete Fourier modes. Its parity blocks are Q=1/2[[I,U],[U*,I]], where U=2Q_EO is unitary. This specifies the ACTUAL cyclic sine/Fourier family U_m, not an arbitrary unitary in the target. The infinite half-density parity operator is the Hilbert/Cauchy unitary. A true finite Toeplitz parity block is only a contraction.

Write p=a+c/2, d=c^2/4, u=p^2-d, v=(1-p)^2-d, q=p(1-p)+d. Generate m independent pairs(Y_i,Z_i) with table [[v,q],[q,u]]. Then send Z through
W_U(z,t)=|det U_(z,t)|^2 if |z|=|t|, and0 otherwise.
Every count-layer block is doubly stochastic. The resulting O=(Y,T) is exactly the noisy2m-site cyclic law. With P_a the input law and Q_a the output law,
H_out=m H_pair+G, G=H_out-H_in>=0.

Define overlap B=|Y intersection Z|, observed count A=|Y|+|Z|=|Y|+|T|,
theta=log(q/v), gamma=log(uv/q^2)<0,
Z_(y,t)(gamma)=det[I_t+(exp(gamma)-1)(U*diag(y)U)_(t,t)].
Output atoms are v^m exp(theta A) Z_(y,t)(gamma).
If lambda_j are the eigenvalues of (U*diag(y)U)_(t,t), the hidden overlap posterior is Poisson-binomial with probabilities
r_j=exp(gamma)*lambda_j/[1+(exp(gamma)-1)lambda_j].
Thus mu=sum r_j, kappa2=sum r_j(1-r_j), |kappa3|<=kappa2.

With input score S=P'/P, acceleration Acal=P''/P and information density R=log(P(X)/Q(O)), the audited exact identity is
G''=E[Acal R]+E Var(S|O).
All weights move with a. The source packet contains the fully expanded all-bias overlap identity; derivatives of those weights cannot be suppressed.

## The unpaid midpoint inequality, with correct normalization

At a0=(1-c)/2, set r=(1-c^2)/4, s=(1+c^2)/4, gamma=2log(r/s), gamma'=0,
gamma''=-64c^2/[(1+c^2)(1-c^2)^2].
S=(A-m)/r is OUTPUT-measurable, so the Fisher loss E Var(S|O) is exactly ZERO. Ordinary Fisher data processing alone therefore cannot solve this task.

Let psi=E[R|O]=gamma*mu-log Z_O>=0 and Abar=E[Acal|O]. The audited identity is
G''=E[Abar*psi]+gamma*gamma'' E kappa2,
H_out''=m[4log(s/r)-2/r]+G''.
The target compensation budget is
E[Abar*psi]+gamma*gamma'' E kappa2 <= m[2/r-4log(s/r)].
A strict margin proportional to m, explicit in c, would be stronger and useful. Both terms are under the ACTUAL output distribution. E[Abar]=0 does not make E[Abar*psi] vanish. The sign of psi does not fix its covariance with Abar. The displayed unpaid inequality is equivalent to midpoint concavity in this representation: merely restating it is not progress.

Known exact obstruction: for m=2 in the actual cyclic family,
G''=8s log[(r^2+s^2)^2/(4r^2s^2)]>0 for every0<c<1,
while H_out''<0. Thus G''<=0 is false and is not your goal. At c=.95, G'' is about17.355 and H_out'' about-122.979. A positive G'' is permitted if the negative iid-pair curvature pays it.

## Create a genuinely weaker useful tool

Use the determinant-overlap structure to produce a bound that can actually fit the budget, not an unrelated norm envelope. Candidate mechanisms include: conditioning on the observable count A to isolate a discrete second-difference/covariance estimate for the posterior divergence psi; a signed integration-by-parts identity exploiting the independent binary-pair INPUT rather than treating the OUTPUT as independent; or a determinantal comparison/majorization for the tilted overlap spectrum averaged under its true output weights. Select a mechanism and derive a checkable lemma strictly weaker than the final sign, with explicit quantitative consequence. If a universal-unitary lemma fails, preserve the obstruction and investigate the consecutive-Fourier/Hilbert structure; a counterexample for an arbitrary U is not automatically an actual cyclic sine counterexample.

Do not treat entrywise positive pair terms as available. Do not compose classical exterior channels without proof: generally W_(UV) is not W_U W_V, because amplitudes interfere before squaring. A sequence of two-mode rotations is not an entropy telescoping argument unless its changed intermediate laws and coherence cost are justified. Generic entropy increase under a doubly stochastic channel is a VALUE fact, not the curvature estimate needed here.

A usable result may instead bound the normalized mixed parity response
v_lim=lim_(n->infinity) (1/n) partial_pe partial_po I_n
in the true model, on a stated parameter domain. Corrected S73 interface: on37/40<c<=74/77 the existing diagonal baseline gives h''<=-1/50-2v_lim, IF the actual-law limit/bridge is justified. Therefore v_lim>=-1/100 suffices there. The range74/77 is ONLY a diagonal-baseline range, not known concavity coverage. For n=2m cyclic models divide by2m and explicitly verify a cyclic-to-true second-response bridge; do not inherit one merely from fixed-gap actual-Toeplitz response convergence. For larger c or arbitraryrho the diagonal input needs replacement; the direct full compensated budget above avoids assuming that baseline.

## Evidence and delivery

State exact input/assumptions/output of your new tool and how it reduces the remaining obligation. Maintain all-size, c and a quantifiers; acknowledge if only midpoint or finite volume is obtained. If using finite-volume curvature, integrate before entropy-value limits where appropriate. Do not differentiate an o(n) value error. Numerical probes can falsify candidate lemmas or guide theory but not prove the all-size or infinite-volume sign. Cite original sources and map every hypothesis when borrowing tools from other fields; do not spend the whole turn on a survey.

Return an English RESULT.md or complete in-chat proof with PROVED/DISPROVED/INCOMPLETE separated, model-valid tests and explicit remaining cost. A source-export failure must not prevent delivering mathematics. Include reproducible sources and actual execution status for computational claims. No SHA/checksum/hash acceptance gate. No other agents, shared-status changes, PR merges or Work routing.
