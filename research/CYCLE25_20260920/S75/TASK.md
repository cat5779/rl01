# S75 — Cycle25 targeted mathematical research

## Research here; no routing to Work

You must NOT attempt to route, redirect, hand off, or instruct the user to use Work, a cloud work task, another chat, or another agent. This is mathematical derivation in THIS chat. Repository access or code execution is not a prerequisite. If a source or export is unavailable, use the explicit formulas here, identify the missing dependency, and deliver complete readable mathematics in chat.

Think for at least 120 minutes, unless you achieve major progress earlier. Do not fabricate elapsed time or idle to fill time. A proved useful structural lemma, decisive model-valid obstruction with a useful replacement, or a closed theorem counts as major progress; a survey, renamed remainder, or positive numerical samples does not.

## Frozen model and success contract

Q_rho(i,i)=rho and Q_rho(i,j)=sin(pi*rho*(i-j))/(pi*(i-j)). Q_{rho,N} is a finite compression of the infinite sine projection, hence a contraction and generally NOT a projection. K_N=aI+cQ_{rho,N}, 0<rho,c<1, 0<a<1-c. H_N is the FULL spatial-configuration Shannon entropy in nats, h=lim H_N/N. Derivatives in a hold c,rho fixed. The long-term target is every fixed c in(37/40,1), every density and every legal a, not only c=.95 or the midpoint. Use a=(1-c)p_noise if useful, explicitly distinguishing noise bias from the observed marginal a+c*rho.

You may first prove a half-density theorem, but state exact scope. A fixed compact legal interval has delta=min_a min(a,1-a-c)>0. Constants may depend on this gap: endpoint uniformity is not demanded for a theorem at every fixed interior parameter, but coverage and costs must be honest. A finite-n sign, count entropy, Tr h_b(K), an auxiliary law, or differentiating an o(N) entropy-value error does not solve the rate problem. Integrating rigorous finite-volume curvature/chord inequalities and then taking entropy-value limits is a valid route when the stated bounds justify it.

Build a mathematical TOOL: specify input class, assumptions, output inequality, exact target interface and a model-valid falsification test. Borrow mechanisms from other fields when useful, checking original-source hypotheses. An unproved equivalent reformulation is not a weaker new lemma. Do not quietly add assumptions or exclude rare words. Report narrow subsidiary results separately if the frozen main obligation remains incomplete.

The universal c<=37/40 baseline is accepted. Previously audited small rectangles near rho=1/3,c=.95 do not cover the whole high-contrast interval and cannot be multiplied together into an unproved product domain. S73's newest parity/exterior-channel manuscript is still under separate review and is NOT an accepted input to this task.

## Your distinct responsibility: actual-weight reveal drift beyond worst-case loss

S74 owns block-information acceleration under the common identity shift. You own a new signed/compensated actual reveal tool after S72, independently accepted in RL01 PR61. Do not just optimize S72's constants, repeat its transfer proof, enlarge enumerations to impossible windows, or re-prove the block-information target.

For observed A and core I subset A subset V, let G_A=(K_A-diag(1-Y_A))^-1. The accepted matrix martingale is E[(G_V)_II|Y_A]=(G_A)_II. One exterior reveal has endpoints M_1=M+vv*/q and M_0=M-vv*/(1-q) with ACTUAL conditional probability q. q,v and the background word are correlated.

For Hermitian X in the actual fixed-word sign domain, h_ij=|X_ij|^2, x_ij=X_ii X_jj,
f_ij=h_ij+(x_ij-h_ij)log(1-h_ij/x_ij),
Phi(X)=sum_i X_ii^2+sum_(i!=j) f_ij (ordered),
Chi(X)=sum_(i even,j odd) f_ij (each cross pair once).
For I=[1,m], A=[1-L,m+L], W_mL=-E Phi((G_A)_II)/m, V_mL=E Chi((G_A)_II)/m.

## Accepted structure and remaining scale problem

Write sigma_i=2Y_i-1, S=diag sigma_i, u_i=sigma_i G_ii. S72 proves (SG+GS)/2>=delta G^2 and E[(SG+GS)/2]=2I, hence E G^2<=2/delta I. Along actual reachable rank-one variations the pair Hessians split into nonnegative same-sign and nonpositive opposite-sign terms. The exact row weights 1/(delta*u_i)-1 and actual q are retained before the final envelope. In the opposite-sign formula, either label the positive-sign index i, or use the sigma_i correction recorded in PR61.

The current proved bound for Psi=Phi or Chi is
E Psi((G_V)_II)-E Psi((G_A)_II)
 >= -Gamma_Psi*eta_delta*E sum_(i in I,r in V\A)|G_V(i,r)|^2,
eta_delta=1/(2delta)-1.
Set kappa=c^2/[4delta(delta+c)], theta=kappa/(1+kappa), beta=(1-2delta)/delta,
A_lambda=2kappa+4theta(1+lambda), B_lambda=theta*beta*(1+1/lambda),
Gamma_Phi=1/2 inf_(lambda>0) max{A_lambda,2B_lambda-2},
Gamma_Chi=1/2 inf_(lambda>0) max{A_lambda/2,B_lambda}.
The improved expected spatial tail is
tauhat(R)=min{2/delta-4,4c^2/(pi^2 delta^3)*(H_(R-1)+2)/R}.
With Clog=max(1,log(1+kappa)), Bhat=2Clog/delta,
eps_Psi=Clog*tauhat(m)+Gamma_Psi*eta_delta*tauhat(L+1),
H_N''/N<=W_mL+eps_Phi+4Bhat(m+L)/N,
h''<=W_mL+eps_Phi.
At the separately justified half-density c=.95 baseline, h''<=-1/50-2V_mL+2eps_Chi.

At c=.95,delta=.02, Gamma_Phi=45.7234744, Gamma_Chi=23.3263933. Observation-tail prefactors are about5.0172753e7 and2.5596248e7, a verified enormous improvement, but still unusable by direct enumeration. The method has reduced fixed-c delta dependence from delta^-10 to delta^-5; that is NOT an endpoint-uniform theorem. At m=L=2 the remaining errors are about105590 and53987, against an absolute local envelope253.61. Even eliminating observation error does not automatically eliminate the separate core/pair spatial truncation term Clog*tauhat(m).

## New obligation: average the signed drift before taking an envelope

For one reveal define Delta_Psi(Y_A)=q Psi(M_1)+(1-q)Psi(M_0)-Psi(M). Universal conditional Delta>=0 is FALSE: the certified six-site actual word00011 at c=.95,a=.025 gives DeltaPhi about-.106585 and DeltaChi about-.096395. Excluding rare words or replacing actual weights by independent signs is illegal.

Independent finite diagnostics in PR58/61 nevertheless find positive UNCONDITIONAL E Delta for tested true-law windows. These are probes, not a theorem. Investigate whether actual-weight signed drift can be proved nonnegative in a clearly specified sine geometry, or whether a nontrivial Bellman/martingale correction gives an explicit globally affordable one-sided payment. Do not silently demand it for every arbitrary contraction. If a proposed averaged sign fails, produce an actual sine counterexample with exact or certified sign when feasible, then state the minimal useful corrected inequality.

Mechanism seeds: a discrete integration-by-parts identity for the joint core/reveal law that cancels opposite-sign Hessians; a self-normalized predictable compensator paid by positive diagonal drift; a multiscale signed sum retaining long-pair terms rather than two independent absolute tails. Construct at least one candidate with explicit input/output and test its strongest claimed quantifier before deepening. Use theory to avoid an enormous all-word computation, not a numerical extrapolation to manufacture a sign.

Any correction must telescope back to ORIGINAL Shannon entropy with its endpoint cost controlled. A local mutual-information identity under a parameter moving only core diagonals holds, but does NOT permit dropping moving halo/background derivatives for the global a path. Explain exactly whether a new averaged sign solves only halo comparison, only core pair truncation, or both, and quantify any unpaid term. A useful intermediate theorem is welcome; a declaration that a remaining equivalent quantity should be positive is not a tool.

Deliver English RESULT.md/full in-chat proof with exact tool/obstruction, complete proof, rho,c,a and geometry scope, legal finite-to-rate interface, remaining budget, and PROVED/DISPROVED/INCOMPLETE separated. Numerical trends remain diagnostics. Supply source and actual run status, without SHA/checksum/hash acceptance gates. No routing to Work, other agents, PR merges or shared-status edits.
