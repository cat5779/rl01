# QWE05: next investment after the independent partial-result audit

Source: DPP PR126. Independent audit: RL01 PR42, separate from QWE06 PR41.

The new verified result is a ONE-SIDED bound:

    J_m <= K (1 + log n) / n,
    sum_m omega_m J_m <= K' sqrt(n) (1 + log n).

This pays the adverse contribution of the interpolation term in the lower
bound for W_rel. It is not an absolute-value bound on J_m or its weighted
sum. Combining the previously reviewed one-sided parameter-response input
leaves the sufficient target sum_m omega_m C_m = o(n), where C_m is the
actual deleted-site conditional KL given the full retained configuration.
That sufficient condition is not necessary: signed cancellation could
also complete the original lower bound.

Renewal recommendation: prioritize one focused creative continuation on
actual deletion-fiber conditional information stability. Require a new
conditional likelihood/Fisher/entropy contraction tool, or a rigorously
scoped obstruction; merely improving corrected deletion-marginal chi-square
or radial KL does not address the remaining conditional information.
An alternative later task could exploit signed cancellation with the
remaining response/interpolation terms, but should be explicitly separated.

The independent reviewer supplied an abstract obstruction: a deletion
Markov map has nonzero kernel directions, so two upper-layer laws can have
identical deleted marginals while their conditional deletion KL is positive.
This rules out an inference from marginal closeness alone. It is not a
Fourier-DPP counterexample and supplies no actual-model asymptotic lower bound.

Do not repeat the now-proved Poisson uniformization, polynomial likelihood
comparison, or interpolation upper-bound work. Do not add a web assignment
until fresh global slot/overlap checks; S59 and S60 were already sent once
and must never be resent automatically. This document is a recommendation,
not a dispatch receipt. No new PRO assignment was sent with this audit intake.
