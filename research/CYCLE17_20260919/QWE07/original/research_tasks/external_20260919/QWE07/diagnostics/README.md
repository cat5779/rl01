# Non-load-bearing diagnostics

These files document exploratory computations and failed/coarse guard attempts.
They are **not** certificate premises and are not actual-entropy counterexamples.
`explore.cpp` is a floating-point exploratory evaluation with no enclosure guarantee.
The old guard log with the 200,000-node inner limit was superseded by the completed
final adaptive run in `../certificates/guard_final.log`.

The rigorous outside-density obstruction uses the complete outward interval
computations in `../certificates/probe_034.log` and `probe_half.log`, not the
exploratory values in this directory.
