# S67 support

## Independent exact-rational core check

```sh
python code/verify_rational_core.py --output certificates/rational_core.json
```

This needs only Python standard-library modules. It certifies the four-site actual-model pairwise counterexample, the original and three-site-improved acceleration bounds, and the scalar constants. `S67_ACCELERATION_RESULT.md` opens with the strengthened local-score allocation theorem; the entropy-rate task remains incomplete.
