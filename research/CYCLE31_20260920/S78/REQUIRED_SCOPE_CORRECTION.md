# Required scope correction from independent RL01 PR68

The first-interaction O(R^-4) defect requires the retained pair (p,i) to remain fixed as the new center q recedes, or an explicit condition |q-i| >= const |p-q|. It is not uniform over moving retained leaves or all pairs of a growing core.

Countercheck: choosing i=q+1 keeps the direct term |b_qi|^4=(c/pi)^4 constant while |p-q| grows. The off-diagonal center-overlap decay alone cannot remove this term.

This correction does not invalidate the separately verified arbitrary-block log-determinant compensation theorem. Preserve the original manuscripts as received and inherit this qualification with the accepted result.

PR68 independently certified both actual-sine raw-reveal counterexamples and reconstructed the star moment certificate. Author exports were not received or replayed. The approximately220-fold reduction concerns a sufficient method's error bound; the final bound remains positive and the entropy-rate theorem remains open.
