# S73 Cycle25 实际复现记录

复现日期：2026-09-20（Asia/Singapore）

## 1. 安全门

在执行前对冻结作者程序做静态风险扫描。没有发现外部通信、进程启动、文件变更、动态代码执行或不安全反序列化。程序只做内存中的有理数、线性代数和枚举运算并打印结果。

## 2. 作者检查器复跑

执行对象：

```text
C:/Users/UIO/Desktop/20260907/s73_checks.py
```

环境：Python 3.12.14，NumPy 2.3.5。进程退出码为 0，全部断言通过。原始输出逐行保存在 `S73_CYCLE25_AUTHOR_CHECK.log`。

复跑得到：

\[
\frac{84286283041}{880843041}>1,
\]

\[
G''=17.354966340795745,qquad
H(Y,T)''=-122.97902406791202.
\]

中点 \(m=1,2,3,4\) 的补偿恒等式残差绝对值不超过 \(3.553\times10^{-13}\)。off-midpoint \((m,c,a)=(3,.95,.023)\) 的分解为

\[
44.911559117443
=44.770270602074+0.141288515369
\]

并有残差 \(-2.112\times10^{-13}\)。

这些浮点值只诊断实现；四点正号的证明证据是有理整数比较。

## 3. 独立复现

`S73_CYCLE25_INDEPENDENT_CHECK.py` 没有复制作者的 NumPy 枚举流程。它只使用 Python 标准库：

1. 以有理正交矩阵
   \[
   U=\begin{pmatrix}3/5&4/5\\4/5&-3/5\end{pmatrix}
   \]
   构造 bipartite projection，精确核对全部 16 组 \((A,B)\) inclusion-correlation 恒等式；
2. 精确构造 \(4\times4\) 外幂通道并检查每行、每列和为 1；
3. 从 \((u,u',u''),(v,v',v''),(q,q',q'')\) 的二阶 jet 独立重建四点 changed atoms；
4. 用 80 位 Decimal 计算对数，但用 `Fraction` 决定严格正号；
5. 精确重算 §9 两个端点常数；
6. 对 \(c=k/1000\) 的网格复算 §8.2 解析表达式，只作为诊断。

独立程序退出码为 0。它得到同一个精确比例，并在高精度下得到

\[
G''=17.35496634079574362857598126060661735747\ldots,
\]

\[
H(Y,T)''=-122.97902406791204366427341428700608272897\ldots.
\]

与作者双精度输出的差别只来自浮点舍入。

## 4. 解析证明与诊断的边界

- §2–§7 的一般性结论由逐式证明审计支持，不由有限测试支持。
- §8.1 在 \(c=19/20\) 的正号有 exact certificate。
- §8.2 对所有 \(0<c<1\) 的负号来自两条解析不等式；网格不承担证明。
- §9 的区间端点由有理计算精确核对，但 mixed response 的下界没有程序或证明证书。
- 没有进行 finite cyclic 到 true Toeplitz/Hilbert 二阶响应的数值外推。

## 5. 复现裁决

作者检查器可复现，独立实现与其一致；这认证有限 cyclic 证书和恒等式诊断。它不认证全尺寸 \(v\) 下界、全区间 \((.925,1)\) 或任意 \(\rho\)。
