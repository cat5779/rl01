# Next round: four Web Pro researchers + selective Sol review

**Origin:** this package was created at the user's explicit request on 2026-09-18 for the **next round of four web-based GPT Pro research sessions and selective GPT-5.6 Sol review**.

This PR is an instruction/prompt package only. It does not launch any researcher, does not change the accepted mathematical status, and does not promote any pending author result. The full fixed-density high-contrast sine entropy-rate target remains open unless a later independently reviewed proof changes that status.

## Why four tasks

The next round should avoid duplicating the currently active S59/S60/S61 work. Before starting, each researcher must read the newest independent reviews of those tasks/results that exist at launch time and inherit any accepted progress rather than re-proving it.

The four Web Pro tasks are deliberately complementary:

1. [WEB_PRO_01](WEB_PRO_01_TRUE_BLOCK_REMAINDER.md): true-law full-block signed payment using the exact retained remainder and actual rank-one reveal path.
2. [WEB_PRO_02](WEB_PRO_02_GENERAL_DENSITY_OFF_MIDPOINT.md): move beyond the half-density balanced benchmark to ordinary density and a fixed off-midpoint interval.
3. [WEB_PRO_03](WEB_PRO_03_DYADIC_FINITE_CHORD.md): finish a true-law finite-chord route by paying the retained signed dyadic scales, using the already reviewed boundary-response tail.
4. [WEB_PRO_04](WEB_PRO_04_HIGH_CONTRAST_ENDPOINT.md): attack the genuinely high-contrast regime directly with an all-orders posterior/low-noise resummation, without requiring n(1-c) to be small.

All four use [COMMON.md](COMMON.md). The public/shared counterexample and failed-route area should be read once at the start; prompts only refer to it briefly rather than duplicating it.

## Sol allocation

Do **not** automatically spend one Sol on each Pro. Use [SOL_PROTOCOL.md](SOL_PROTOCOL.md).

Recommended policy:

- keep Sol 1 unused until the first result appears that could change the research allocation;
- use Sol 2 only for a second independent load-bearing result or a parameter-extension theorem;
- reserve Sol 3 for the final composition/coverage audit if two or more Pro outputs are meant to combine.

A Pro author's `PROVED` label is not certification. Sol reviews should rebuild the new load-bearing chain, not merely summarize it.

## Dispatch rule

Launch the four Web Pro prompts independently. Do not force them to converge on one mechanism. If a newly reviewed S59/S60/S61 result already closes one prompt's stated bottleneck before launch, the corresponding Pro should use that result as an accepted input and attack the **next unpaid statement explicitly named in that prompt**, rather than repeating the closed step.

No prompt authorizes merging, changing `README.md`, `PROGRESS.md`, `STATIONARY_STATUS.md`, or any accepted-claim ledger.
