# 本轮自审记录

状态：`SELF_AUDIT_PASSED / INDEPENDENT_REVIEW_PENDING`。按研究级协议，自审不能替代新上下文验证。

## 解析核对

1. 重新积分 `int_0^1 (1-t)h^2/(v-th) dt`，与 `Psi(v,h)` 完全一致。
2. 逐分支展开后验更新，确认交叉储备的一次项由 `E gamma=0` 消失，二次项系数为 `c^2/[p(1-p)]`。
3. 由通道全方差式核对 `p(1-p)-c^2q(1-q)>0`，并在平衡点化为常数 `beta=(1-c^2)/4`。
4. 核对 `Q_j/D_j<=kappa` 与 `kappa/(1+kappa)=c^2`，没有把条件不等式误写成逐分支不等式。
5. 核对 pair-cut 的因子：无序 Hessian 因子 2 与有序后验能量恰好相消；块指标求和对每条有序跨块边计数两次。
6. 核对 `V_m` 公式及单调性；端点块用 `V_r<=V_m`，块数用 `n/m+2`。

## 执行检查

- `enumerate_phi.py`：复现 `H_6''=-49.565521239251...`、`W_(4,2)=-10.13300431506281...`。
- `benchmark_reduction.py`：重建所有基准有理常数，并修正报告表格中的三处初始手算近似。
- `verify_reductions.py`：三个真实 sine 窗口的 pair-cut 两级上界通过；十个随机严格收缩核的完整揭示树通过 `sum QV<=c^2 S0`。
- 三个脚本均通过 Python 字节码编译。

## 尚未验证

- S18 缺失附件中的有限两分支 Jensen 引理；
- `9/16(1+m/L)` 的原始证明；
- 任意完整净负窗口；
- 熵率邻域或 chord 转移。
