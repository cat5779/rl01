# PRO02 — Round 2: turn the exact-potential improvement into a complete curvature certificate

Think for at least 2 hours, unless you achieve major progress earlier. Do not fabricate elapsed thinking time. The small-calculation restrictions and mandatory heavy-computation handoff remain withdrawn. Use suitable mathematical and computational tools. No SHA256 or checksum gate.

Repository: cat5779/rl01
PR: https://github.com/cat5779/rl01/pull/3
Branch: research/pro02-sa02-rankone
Original assignment and inputs: research/PRO02/PROMPT.md and research/PRO02/inputs/
This follow-up: research/PRO02/ROUND2.md

Continue from your new exact logarithmic potential Phi, true two-branch payment, posterior cross-storage, and complete localization bound. These are your new submissions, not independently certified inputs. Preserve the complete previous report and scripts; if you discover a specific defect, repair it explicitly or state subsequent results conditionally. Do not spend the whole round repeating the old SA02 audit.

Your reported benchmark reduces the sufficient inequality to
Wcal_(m,L) + C_* h_m/m + (50251200/2197)(1/m+1/L) < 0.
The reported Wcal_(4,2) approximately -10.133 is exploratory and does not pay these errors. Merely extending floating-point enumeration does not close the theorem.

Primary goal: construct and prove a mechanism that actually closes a complete negative-curvature certificate at rho=1/2,c=19/20,a=1/40, or produces a rigorously quantified further reduction of the remaining obstacle.

Prioritize the actual finite-window expectation and the sine-specific storage cost. Possible independent routes include:
- an analytic lower bound for E Phi that survives increasing the core and observation window;
- a multiscale/telescoping or overlapping-block construction that retains signed cancellation while paying all boundaries;
- a state-dependent supply/storage inequality exploiting sine-realizable posteriors, rather than the uniform strict-DPP worst case.

These are design seeds, not a requirement to complete every route. Transfer a concrete mechanism from outside DPP theory when useful, and prove its hypotheses. Do not merely rename the unpaid window term. Your obstruction for one unweighted general-DPP payment class does not rule out a weighted supply or a sine-specific argument.

For any candidate certificate, explicitly include the full pair-cut tail, observation-domain change, finite endpoints, and actual word weights. If you use rigorous computation, state the finite input and certify every error affecting the sign. A negative internal block or a high-precision decimal is insufficient. If the result only proves a finite-volume Hessian inequality, state that scope. Entropy-rate concavity requires a justified uniform neighborhood/chord argument or a valid limit-and-derivative transfer; do not infer it from one point.

Deliver a self-contained English ROUND2_RESULT.md with the new object, full proof, explicit benchmark inequality and margin if closed, and the precise residual obstruction otherwise. Preserve the new first-round RESULT and evidence as separately identifiable artifacts. Provide readable full proof text or accessible files, not only attachment cards and a summary. Continue independently of the other PROs; a fresh context will later audit the new result.
