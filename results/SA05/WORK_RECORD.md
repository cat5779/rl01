> **待独立对抗性审查 / PENDING_INDEPENDENT_REVIEW。** 下文保留来源原文；其中 PROVED、PASS、AUDIT 等是作者或既有记录的表述，不代表本次 SA 成果已独立通过。先读本包根目录 REVIEW_REQUEST.md；外部审计与其他 SA 分开进行。

# SA05｜实际工作与交付记录

## 工作窗口

起始记录：2026-09-17 09:13:19.680398 UTC。  
交付前记录：2026-09-17T11:03:20.326989+00:00。  
两次记录之间的实际工作窗口：110.01 分钟，超过 110 分钟。

该窗口覆盖本次理论推导、文献条件核对、反例论证、证明审计、必要小算与 GitHub 交付。
这是壁钟工作窗口，不是 CPU 用时，也不是独立评审时长。
没有用定时等待或后台任务替代当前工作的交付。

## 范围和产物

仅在指定 PR #118 的分支
`research/sine-entropy-tool-3-round-prompts`
新增/更新本任务目录 `research_prompts/pro_tasks/results/SA05/`；
没有合并 PR、没有强推、没有改动其他任务结果或原始任务/源材料。

主文交付全模式 Jacobi 方程、Duhamel 残差、完整移动参考 Hessian、
中点和明确邻域的单侧工具、条件相对熵层内加速的 O(1) 支付、
真熵梯度源的 O(sqrt(n)) 支付、n=8 合法模型的失败机制及精确剩余义务。
跨层补充交付下采样参数响应、两步 jets、匹配热时间、
跨层非线性 KL 账本，以及 n=8 中点移动相对熵权重项严格为负的解析证书。

证明状态与目标状态分开：
上述具体引理有正文证明；目标级 E_n'' >= -o(n)、中点全 n 符号、
n=8 全 Hessian 的中点符号仍未完成。
存在型合法正/负曲率反例没有被冒充为已定位的数值坐标。
没有外部新颖性优先权或独立同行评审的声明。

## 实际小算与环境

Python 3.13.5，SymPy 1.14.0。
两份检查程序均实际成功执行，标准输出均为 `EXACT_PASS`；
随附 JSON 是成功执行的输出，不是手工伪造的预期结果。

主检查：n=4 的 6 个输入集合和全部 16 个输出原子；
层归一、完整归一、条件一二阶 jets、完整原子与移动层 Hessian 缺陷一致。
另检查一般移动 Bregman Hessian、两个正有理密度的图耗散和二阶调制恒等式，
以及 n=8 的单原子度四证据、低次多项式和五值重叠 score。

跨层检查：只在 k=4、m=2,3,4 上做有理多项式恒等式核对；
核归一、模式连续关系、移动 eta 导数、两步二阶 jets 和匹配时钟关系。
没有枚举 n=8 的完整层或完整配置。

一次未化简的跨层符号运算超过 45 秒，未产生成功结果；
对同一低次有理表达式先作约分后，成功复跑，没有扩大问题规模。
交付前复跑时，环境的表格运行时预热曾报错，旧 JSON 的文件所有权还阻止了覆盖；
随后不带输出路径运行两份程序，均以返回码 0 输出 `EXACT_PASS`，
再以原子替换保存实际标准输出。数学断言没有因这些环境问题而被跳过。

## 文献核对

主文列出相对熵后验分析、slice 谐波基、Jacobi 正交多项式零点，
以及齐次强对数凹 down-up 谱隙的原始来源。
外部谱隙用于真实条件律；没有假定 corrected law 也强对数凹。
原始论文的适用条件和本题生成元归一化分别核对；
研究 PDF 的相关页面已作图像查看。
没有把已有论文包装成本题参数 Hessian 的现成定理。

## 交付限制与后续计算

未执行大配置枚举、密集参数扫描、成长体积拟合或旧六点证书重算。
未做哈希校验；GitHub 写入所需的版本标识只作为 API 的文件更新元数据使用。
本地做了数学定界符、相对文件链接和文件存在性检查。

`SA05_HANDOFF.md` 给出未执行计算的精确输入、完整配置公式、误差认证标准和理论升级接口。
压缩包包含完整模型定义、证明、跨层补充、审计、计算交接、可复现小算与实际结果；
除 Python/SymPy 运行依赖外，阅读证明不要求在线访问仓库。
