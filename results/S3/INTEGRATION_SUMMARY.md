# S3 integration summary

- **S3 — ACCEPTED (scoped).** Accept the exact finite latent-prefix decomposition and fixed-interior full-moving-law response theorem: for fixed `0<c<1`, `0<eta<(1-c)/2`, and normalized value gap `epsilon`, the normalized Hessian gap on `[eta,1-c-eta]` is at most `C_(c,eta) epsilon[1+log(1/epsilon)]^3` (with the zero-gap case handled separately). Combining this with the already accepted sine-block boundary budget gives `F_(Q_n)''-G_(n,m)''=o(n)` uniformly on fixed interior intervals for `m=floor(sqrt(n))`. This does not sign `G_(n,m)''`.

- **S3 — ACCEPTED (local lemma).** For every negatively correlated binary pair passed independently through a symmetric channel with crossover `0<epsilon<1/2`, accept the exact negative-log-odds bound in Lemma 5.1 of `AUTHOR_PROOF.md`. It is a scalar local payment and does not provide a compatible global Fisher allocation.

- **S3 — ACCEPTED (method obstruction).** Accept the exact rational six-coordinate rank-three projection witness at `(a,c)=(1/200,99/100)`: the proposed once-only pooled atomwise payment has defect in `(1.2248629717184887,1.2248629717184890)`, hence greater than `6/5`; direct sums of `k` copies have defect greater than `6k/5`. The complete 64-atom entropy Hessian of the base example is below `-1000`. Therefore this is only a pointwise payment obstruction. It is not an expected-curvature counterexample, not a contiguous Fourier construction, and not a continuous Fourier counterexample.

- **S3 — CRITICAL OPEN OBLIGATION.** Prove an `o(n)` upper bound, or equivalent finite-Jensen bound, for the actual conditioned leaf sum `G_(n,m)''` of the finite sine Toeplitz compression. The latent-conditioned block kernels are general finite contractions. No submitted result proves or disproves fixed-density high-contrast sine entropy-rate concavity.

Audit: `research/s3-latest-reviewed-20260917/INDEPENDENT_AUDIT.md`.
