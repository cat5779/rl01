> **待独立对抗性审查 / PENDING_INDEPENDENT_REVIEW。** 下文保留来源原文；其中 PROVED、PASS、AUDIT 等是作者或既有记录的表述，不代表本次 SA 成果已独立通过。先读本包根目录 REVIEW_REQUEST.md；外部审计与其他 SA 分开进行。

# SA03 计算与理论交接

**以下后续工作未执行。** 本次只运行最小精确符号脚本；第三个复用相同 R=1 四个 word，不扩大模型。有限代数检查不证明成长体积结论。

## 1. 计算实际局部残差，而非两个 order-R 数相减

输入严格为真实 Fejer Toeplitz DPP：c=19/20、正奇数 R、u in [1/(R+1),1]、delta=0、固定 X=(M-R)/sqrt(R)。对完整外部 word 使用实际概率，不以计数律或谱 Bernoulli 配置代替。

给定 word 后求条件事件逆 G，用秩一式生成一次/二次翻位，计算

\[
\mathcal R=u^2(\phi''(q)-8)+u\mathcal B_{\phi'}
 +\partial_\delta\mathcal B_\phi+T\mathcal B_\phi.
\]

分别记录四项期望及总和，保持全部 u 因子，用原始完整 jets 核对。R=1 已精确通过，无需扩大枚举只为重复确认。

有限 R 判号需覆盖全部 word 聚合及 u 积分的认证区间，最终区间严格离开 0。浮点只作诊断，一些有限 R 为正不证明成长 R 的符号。重型枚举、扫描、优化及批量区间认证不属于本次已执行部分。

## 2. 面向无限体积的可认证近似

定量附录给 E_c(R;m,d,M) 和 Gamma 的显式误差，参数须满足 2<=m、2m<=d<=R/2、2m<=M<=R/2。可以结合有限观察函数的真实有限边缘求期望和 u 积分，再支付误差。

常数极度保守，直接照搬很可能不实用。优先改进带赔率的行能量和局部尾，不以经验 R 拟合替代证明。Gamma 的认证区间严格为正才支持 eventual positivity；区间穿过 0 时仍未定。

## 3. 区分合法与失效的符号闭合

真实 R=1 已否定 E[T Bcal_phi]>=0：

\[
T\mathcal B_\phi=-16u^2t^2/(1-4t^2)<0,
\quad t=c^2u^2/\pi^2.
\]

不要再把它作为候选正耗散定理。结构补充 S6 显露正预算 2u^2 sum_i o_i B_i 和三个混合项，应比较整体，而非要求每项非负。

## 4. 已完成的升级与剩余理论目标

结构补充 S1–S3 给出速率身份、二变量缺陷和逐 word 首项。原 S4 的复数双翻位缺口现已由 `SA03_UNIFORM_WEAK_NOISE.md` 付清，得到对 R 一致的弱耦合（小 cu）余项，无需再为这一身份做重型计算。

剩余目标是 c=19/20、u 接近 1 时的混合项有符号比较、Gamma(19/20) 的符号、尖锐体积误差与真实熵率桥接。弱耦合系数不能外推支付高有效对比度区。
