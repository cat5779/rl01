# Independent audit of S11

**STATUS: ACCEPTED (scoped).**  The packet correctly rejects two precisely
defined continuation bookkeeping families.  It neither proves nor disproves
entropy concavity at `c=0.926`.

## Accepted statements

1. In the declared split-tail certificate family, the unavoidable head-value
   term is larger than `24 M^2/ell^2` per contrast unit after granting
   `sigma=1` and `Lambda_delta>3`.  The threshold function is increasing, and
   the exact 100-bin cover validly combines the right-endpoint cutoff floor with
   the left-endpoint interval-length ceiling.  Its minimum is
   `366368000000/1323`, yielding the two quoted total-width ceilings.  The lower
   bound adds over an adaptively reoptimized chain.
2. The quadratic and quartic trace calculations give
   `u_(s,1)''=-12/(1+delta)^2` and equation (4.13) for `u_(s,2)''`.  For the
   complement-reduced class, `Q=diag(1,0)` attains the maximum local mode-two
   profile.  Throughout `0.925<=d<c<=0.926` and every legal shift, it forces at
   least `(4177920000/47458321)(c-d)` of nonnegative mode-two payment.
3. For the same witness, the complete centered entropy response across the full
   benchmark step is `-3125000/8800857`.  Thus the positive mode-two response is
   genuinely cancelled by higher modes; the witness is not a target
   counterexample.

## Scope limits

- Family A is an obstruction conditional on using the rejected checkpoint's
  declared split-tail formula.  This review does not promote the old tail lemma
  from rejected history into an accepted theorem.
- Family B assumes nonnegative payments are assigned mode by mode against the
  single uniform reviewed base margin.  Signed mode blocks, a direct response
  estimate, or a same-witness excess-margin argument are outside its scope.
- Calling signed cancellation “mandatory” is justified only within these
  continuation architectures, not for every possible proof of the target.

## Replay

The standard-library package validator passed all exact rebuild, recheck,
two-site, document-constant, cover-refinement, integrity, and manifest checks.
The optional SymPy script was unavailable in the specified runtime; the
load-bearing formulas were checked from the displayed trace expansion and the
exact witness certificate does not depend on SymPy.

