# S65 — Cycle11 independent mathematical tool research

Think for at least 120 minutes, unless you achieve major progress earlier. Do not invent elapsed time or idle merely to fill time. Work in this chat and return a complete readable proof even if file export fails.

This is one assignment, adapted from DPP PR128. Work independently on this route; do not seek unfinished work from the other researchers. Create a reusable mathematical tool with explicit inputs, hypotheses and a proved output interface. Give priority to theoretical derivation and transferable mechanisms from other fields. Check primary sources and their hypotheses when importing a tool; do not assume the conjecture is true. Report PROVED, DISPROVED, or INCOMPLETE separately for each new claim and for the overall objective. Do not quietly change the target's assumptions; a weaker useful theorem must be explicitly labeled partial.

## Cycle12 launch update — before the first send

Do not route the user to Work; this is a mathematical derivation task. Carry out the research in this chat.

The 120-minute request is substantive: a new name, elementary identity, finite-n expansion, or an untested tool with the same unpaid target is not major progress justifying early completion. A major-progress claim must exhibit a proved result that strictly and substantially improves an applicable existing baseline or closes a genuine missing obligation, with its internal checks completed. Do not fabricate duration or idle to fill time. Keep attacking the stated endpoint problem rather than stopping after a convenient weak lemma.

Fresh intake status is in RL01 PR45, research/CYCLE12_20260919/SOURCE_STATUS.md, on the same branch and raw-file base listed below. S62/S63 are being continued by the user and must not be contacted; their initial claims are not accepted new inputs. S64's proposed KL supermodularity tool is under independent audit and does not pay full entropy acceleration. None of these changes closes your endpoint target.

An OPTIONAL additional seed is the full visible S52 synthesis at research/CYCLE12_20260919/S52/S52_ENDPOINT_DISCUSSION.md. Treat it as author discussion, not a reviewed theorem or an instruction to replace this task. In its notation p is your t. Independently verify any imported identity. With an independent Bernoulli(epsilon) resampling mask E and Bernoulli(t) refresh bits, one may use the same actual channel to derive

H(Y)=E_E H(X_(E^c))+n epsilon b(t)+I_t(E;Y).

This representation alone just renames the target. If you use it, construct an actual-output compensated interaction or an all-order posterior estimate with a volume-uniform bound at the required Jensen scale. The fixed-n full-support first-defect cancellation does not supply an n-uniform remainder, and relative entropy of a single defect against a fixed-cardinality projection endpoint can be infinite. Keep the cyclic-support and true-Toeplitz distinctions explicit. A two-defect coefficient without a proved uniform remainder or rigorously useful failure scale is not the desired theorem.

Do not inherit the discussion's empirical inverse-moment growth, fitted rigidity exponents, a claimed logarithmic endpoint law, or novelty claims. No original experimental scripts were supplied for those statements. The reviewed S51 endpoint work already exists; compare against its actual bound before claiming improvement. The original PR128 endpoint target and all-order/true-law requirements below remain the objective. You may abandon this optional seed if it does not help.

## Exact attachment locations

Public repository: cat5779/rl01
Public PR: 45
Branch: research/sa-cycle11-pr128-20260918
Task file: prompts/CYCLE11/S65.md
Source directory: research/CYCLE11_20260918
Raw-file base: https://raw.githubusercontent.com/cat5779/rl01/research/sa-cycle11-pr128-20260918/
Append each repository-relative path below to that base to read the complete file. These sources are published on the branch even if its PR is not merged. If a source cannot be opened, identify exactly which one; do not fabricate its contents.

Required reading, in order:
1. research/CYCLE11_20260918/SOURCE_STATUS.md
2. research/CYCLE11_20260918/sources/FAILED_ROUTES.md
3. research/CYCLE11_20260918/sources/HIGH_CONTRAST_RESUMMATION.md
4. research/CYCLE11_20260918/sources/HIGH_CONTRAST_UNIFORM_ROUTE.md
5. research/CYCLE11_20260918/sources/S61_REVIEW.md
6. research/CYCLE11_20260918/S61/S61_CYCLE10_RESULT.md

The original PR128 proposal is archived at research/CYCLE11_20260918/PR128_original/WEB_PRO_04_HIGH_CONTRAST_ENDPOINT.md. Current independent S61 audit is RL01 PR44 and copied at research/CYCLE11_20260918/sources/S61_REVIEW.md. The status memo governs the scope of all inherited facts. Other packet sources are optional, not a requirement to re-audit the whole project.

## Launch-time corrections and fresh audited input

S61 supplies a verified posterior coupling/truncation tool at fixed c=.95, not endpoint-uniform constants. Inspect its original if transferring the mechanism. In that manuscript the tilt z=(1+xi)/xi=((1+c)/(1-c))^2=1521 at c=.95; do not use a shorthand unsquared ratio from review prose as that z. Track all variance/likelihood/window constants as epsilon tends to zero. Uniform-slice and Fourier-comb formulas are comparison/background only. The actual contiguous Fourier posterior, its spatial entropy defect, and the true-law Jensen transfer all need payment.

# COMMON — requirements for the next four Web Pro tasks

## Ultimate mathematical target

For the actual sine Toeplitz kernel
\[
Q_{\rho,n}(i,j)=
\begin{cases}
\rho,&i=j,\\
\dfrac{\sin(\pi\rho(i-j))}{\pi(i-j)},&i\ne j,
\end{cases}
\]
prove, for every fixed
\[
0<\rho<1,\qquad 0<c<1,
\]
that the complete-configuration Shannon entropy rate
\[
h_{\rho,c}(a)
=\lim_{n\to\infty}\frac1n
H\!\left(\operatorname{DPP}(aI+cQ_{\rho,n})\right)
\]
is concave for the whole legal interval
\[
0\le a\le1-c.
\]

The finite Toeplitz compression is generally **not** a projection. \(\operatorname{Tr}b(K)\) is not this classical configuration entropy. A corrected cyclic law is not the true Toeplitz output.

## Current baseline and inheritance rule

At the time this package is written, the accepted universal whole-interval threshold remains \`c <= 37/40\`. Treat later independently reviewed improvements as inherited inputs if they exist at launch time.

Before doing new work:

1. read the current repository status and the newest independent reviews relevant to your task;
2. read the shared/public counterexample and failed-route area once;
3. do not re-prove an already independently accepted theorem merely because it sits on an unmerged branch;
4. do not treat an author-only claim as accepted without its review.

The shared counterexample area already records failures of pointwise pair payment, layerwise positivity, nodewise completion signs, frozen/reference shortcuts, value-to-Hessian differentiation, and general-complex-kernel concavity. Mention the relevant obstruction when needed; do not spend the round reproducing the catalogue.

## Research standard

This is a mathematical research assignment. Create a reusable mathematical tool or prove/refute a sharply stated load-bearing claim.

Acceptable outputs include:

- a full theorem that closes the stated bottleneck;
- a strictly scoped theorem that removes a real part of the remaining burden;
- an exact/model-valid obstruction that kills a proposed mechanism and identifies the surviving weaker target.

Not acceptable as the main output:

- renaming the unknown remainder;
- a generic identity equivalent to the target;
- a numerical sign scan without a proof/certificate;
- a tiny threshold improvement obtained only by tuning old constants;
- a statement that silently drops moving probability weights, probability acceleration, tails, leakage, endpoint layers, or reference-law derivatives.

Use computation when it clarifies the mathematics. Exact or interval computation may certify finite claims. Floating diagnostics are discovery evidence only.

## Finite chords are allowed and often preferred

For
\[
\operatorname{Gap}_{\lambda}F
=F((1-\lambda)a_0+\lambda a_1)
-(1-\lambda)F(a_0)-\lambda F(a_1),
\]
a finite-volume result
\[
\operatorname{Gap}_{\lambda}H_n\ge-o(n)
\]
combined with an already justified value-limit bridge can prove the corresponding entropy-rate concavity statement. You do not have to prove \(C^2\) regularity if a rigorous finite-chord route is cleaner.

## Default benchmark

Unless your task says otherwise, begin at
\[
\rho=\frac12,\qquad c=\frac{19}{20},
\]
and use a fixed nondegenerate legal \(a\)-interval such as
\[
\left[\frac1{50},\frac3{100}\right].
\]

A midpoint-only statement is not a fixed-interval statement. A fixed-\(n\) continuity argument does not create a volume-uniform interval.

## Delivery

Return a readable proof before optional packaging. For every new claim state:

- \`PROVED\`, \`DISPROVED\`, or \`INCOMPLETE\`;
- exact hypotheses and quantifiers;
- what prior reviewed inputs are used;
- where the new argument enters the target ledger;
- what remains unpaid.

Do not merge your own work or edit repository-wide status files.


# WEB_PRO_04 — High-contrast endpoint route with all-order posterior resummation

The common requirements are included below.

## Motivation

The final contrast range is \(0<c<1\). Small extensions of the current threshold do not by themselves explain the regime \(c\uparrow1\).

Use
\[
c=1-\varepsilon,\qquad a=\varepsilon t,
\qquad 0<t<1.
\]

Do not assume \(n\varepsilon\ll1\). Fixed-volume low-noise Taylor series are known to become nonuniform at positive density because the number of exchanges is order \(n\).

The repository already contains an exact all-orders hypergeometric resummation for the **uniform fixed-cardinality** input. That model is a guide and a comparison law, not the true contiguous Fourier projection.

## Single objective

Construct a volume-uniform, all-orders posterior comparison for the actual fixed-density contiguous Fourier projection and use it to prove a genuine high-contrast true-law concavity region.

A concrete first target is:

for every fixed \(\rho\in(0,1)\) and every fixed \(\delta\in(0,1/2)\), prove that there exists
\[
\varepsilon_0(\rho,\delta)>0
\]
such that for
\[
0<\varepsilon<\varepsilon_0,\qquad
t\in[\delta,1-\delta],
\]
the actual sine entropy rate is concave in \(a=\varepsilon t\), preferably with a quantitative Jensen margin.

It is acceptable to start with \(\rho=1/2\) if the method clearly retains the density parameter.

## Exact identity worth exploiting

For a projection-DPP input \(X\) of fixed size \(k\) passed through the copy/refresh channel, conditional independence gives an exact entropy ledger of the form
\[
H(Y)=H(X)
+(n-k)b(\varepsilon t)
+k\,b(\varepsilon(1-t))
-H(X\mid Y).
\]

The independent noise terms are explicit. The difficulty is the actual posterior entropy \(H(X\mid Y)\).

Build an all-order tool for the posterior rather than truncating by the number of exchange errors.

## Promising directions

Possible mechanisms:

- compare the true Fourier-minor posterior to the uniform-slice hypergeometric posterior by a spatial relative entropy or transport cost;
- derive a cluster/polymer representation whose **activity is volume-uniform after resummation** rather than termwise in the number of exchanges;
- exploit strong Rayleigh / stable-polynomial structure only where its exact hypotheses hold;
- prove a posterior entropy Jensen bound directly from a finite-volume variational principle.

Any comparison must retain actual Fourier minor weights. A statement about only the overlap count is insufficient unless the conditional spatial entropy defect is separately paid.

## Endpoint and parameter discipline

A theorem only for \(t\in[\delta,1-\delta]\) is useful, but keep the \(\delta\)-dependence explicit. State exactly what remains near \(t=0,1\).

Do not let \(\rho\) tend to zero with \(\varepsilon\) and call that a fixed-density theorem.

Do not replace the actual projection by a uniform \(k\)-slice unless the comparison error is proved at the needed Jensen scale.

## Success criterion

Best: a high-contrast theorem with \(c\) arbitrarily close to one at every fixed positive density and on a nondegenerate rescaled \(t\)-interval.

Useful partial: a new volume-uniform posterior comparison that converts the actual Fourier-minor problem to the known all-orders uniform-slice formula plus an explicit \(o(n)\) Jensen error.

A finite-\(n\) low-noise expansion without uniform remainder is not progress toward this task.


## Deliverables

Return S65_CYCLE11_RESULT.md with a self-contained theorem/proof or exact scoped obstruction, a dependency ledger distinguishing reviewed inputs from new proofs and open obligations, and the precise remaining gap. Optional checks may accompany the mathematics. No checksum/manifest is required and packaging failure must not suppress the readable result. Do not merge, edit repository-wide status, send messages to other researchers, or start another task. Do not claim full entropy-rate concavity from only one paid term.
